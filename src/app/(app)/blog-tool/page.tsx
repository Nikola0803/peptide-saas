import { StubPage } from "@/components/stub-page";

export default function BlogToolPage() {
  return (
    <StubPage
      title="AI Blog Tool"
      subtitle="Generate and publish SEO content per brand"
      icon="ri-article-line"
      body="This will generate draft posts per brand (grounded in the master product catalog) and publish straight to that brand's WordPress via the plugin's REST connection."
    />
  );
}
