import { StubPage } from "@/components/stub-page";

export default function SocialAnalyticsPage() {
  return (
    <StubPage
      title="Social Analytics"
      subtitle="Performance across each brand's social channels"
      icon="ri-bar-chart-grouped-line"
      body="This will aggregate reach/engagement per brand and correlate posts with order spikes, once each brand's social accounts are connected."
    />
  );
}
