import { StubPage } from "@/components/stub-page";

export default function EmailMarketingPage() {
  return (
    <StubPage
      title="Email Marketing"
      subtitle="Campaigns and flows built from the unified contact list"
      icon="ri-mail-send-line"
      body="This will let you segment the Contacts list (by brand, LTV, last order) and send campaigns, or trigger flows like win-back and post-purchase follow-up."
    />
  );
}
